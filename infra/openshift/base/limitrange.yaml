apiVersion: v1
kind: LimitRange
metadata:
  name: tradeops-defaults
  namespace: tradeops
spec:
  limits:
    - type: Container
      defaultRequest:
        cpu: 50m
        memory: 64Mi
      default:
        cpu: "1"
        memory: 1Gi
