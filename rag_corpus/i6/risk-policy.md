# Deterministic Risk Policy

The deterministic I3 risk gate is authoritative for hard limits. A VETO is terminal for the agentic layer and cannot be overridden by an LLM, RAG passage, ML score, or specialist-agent agreement.

A REVIEW result is not an approval. It requires later Human-in-the-Loop handling. Agent workflows may explain the reason but must not transform REVIEW into execution permission.
